function Hero({ name, title }) {
  return (
    <section className="hero">
      <h1>{name}</h1>
      <h3>{title}</h3>
      <p>Welcome to my personal portfolio website.</p>
    </section>
  );
}

export default Hero;